# Recommended GitHub Repository Settings

These settings cannot all be represented as files. Apply them in the GitHub repository UI or API when available.

## About

**Description**

> Bare-metal AI unikernel: direct Intel Gen9 GPGPU + AVX2 transformer inference on x86_64, documented with real-hardware proof.

**Website**

Leave empty unless an official AetherOS project site is created. Do not point to an unrelated profile/page merely to fill the field.

## Recommended topics

Use a focused set rather than keyword spam:

- `aetheros`
- `bare-metal`
- `operating-system`
- `kernel`
- `rust`
- `x86-64`
- `artificial-intelligence`
- `llm`
- `inference`
- `gguf`
- `gpu-compute`
- `intel-gpu`
- `intel-gen9`
- `avx2`
- `systems-programming`
- `unikernel`
- `edge-ai`
- `on-device-ai`

## Features

Recommended:

- Issues: **On**
- Discussions: **Optional / On** if you want research conversation separated from bugs
- Wiki: **Off** — docs should remain versioned in-repo
- Projects: optional
- Sponsorship: only if intentionally configured

## Pull requests

Recommended:

- Allow squash merge: **On**
- Allow merge commits: optional
- Allow rebase merge: optional
- Automatically delete head branches: **On**

## Branch protection for `main`

Recommended once the new documentation baseline is merged:

- require pull request before merging;
- require conversation resolution;
- block force pushes;
- block branch deletion;
- require status checks if a docs-quality workflow is later added.

## Social preview

Create a 1280×640 image with:

- AetherOS wordmark;
- **Silicon Sovereign** subtitle;
- “Bare-Metal AI · Intel Gen9 · AVX2 · Rust”;
- abstract silicon/ring-buffer/transformer visual;
- no unverifiable performance superlatives.

## Pinned repository presentation

For a portfolio profile, pin this repository and use a short profile description that matches the technical claim discipline of this README.
