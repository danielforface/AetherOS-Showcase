# HAR — Steady Qualification Evidence

Start with `REPORT.md`.

Then inspect:

- `AUDIT_REDUNDANT_WORK.md` — why HAQ's first-candidate measurement was contaminated.
- `ROUND18HAR_COLLECT.ps1` / `ROUND18HAR_COMMON.ps1` — collector and qualification logic.
- `HAR_RESULT.json` — parsed physical result.
- `HAR_AETHLOG.TXT` — raw physical telemetry.
- `collector.log` — collector output.

The public performance number is 7.0775 decode tokens/sec for the tested configuration. The first request remains visible and slower.
